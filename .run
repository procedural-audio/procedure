clear

if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    cd build/package/linux
    # pasuspender ./standalone
    ./workstation
    cd ../../../
elif [[ "$OSTYPE" == "darwin"* ]]; then
    RUST_BACKTRACE=full ./build/out/host/release/macos/standalone/NodusStandalone_artefacts/Release/NodusStandalone.app/Contents/MacOS/NodusStandalone    
else
    echo "Unknown platform"
fi

