clear

if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    cd build/package/linux
    # pasuspender ./standalone
    ./workstation
    cd ../../../
elif [[ "$OSTYPE" == "darwin"* ]]; then
    ./build/out/host/release/standalone/macos/NodusStandalone_artefacts/Release/NodusStandalone.app/Contents/MacOS/NodusStandalone
else
    echo "Unknown platform"
fi

