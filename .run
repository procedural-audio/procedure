clear

if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    cd build/package/linux
    # pasuspender ./standalone
    ./workstation
    cd ../../../
elif [[ "$OSTYPE" == "darwin"* ]]; then
    ./loader/Builds/MacOSX/build/Debug/flutter_juce.app/Contents/MacOS/flutter_juce
else
    echo "Unknown platform"
fi

