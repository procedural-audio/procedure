/**
    A simple synth using sine oscillators, showing use of the voice allocator,
    message converter and envelopes
*/

// Determine the note frequency from the pitch (midi note)
processor Div [[ main, width: 80, height: 60 ]]
{
    input value float32 a [[ pinTop: 10 ]];
    input value float32 b [[ pinTop: 10 + 25 ]];
    output value float32 c [[ pinTop: 10 ]];

    void main()
    {
        loop {
            c <- a / b;
            advance();
        }
    }
}
