import 'package:flutter/material.dart';

// Color constants
class AppColors {
  static const primary = Color.fromRGBO(0, 122, 204, 1.0);
  static const background = Color.fromRGBO(20, 20, 20, 1.0);
  static const surface = Color.fromRGBO(25, 25, 25, 1.0);
  static const sidebar = Color.fromRGBO(20, 20, 20, 1.0);
  static const border = Color.fromRGBO(30, 30, 30, 1.0);
  static const textPrimary = Colors.white;
  static const textSecondary = Color.fromRGBO(187, 187, 187, 1.0);
  static const textMuted = Color.fromRGBO(128, 128, 128, 1.0);
  static const hover = Color.fromRGBO(40, 40, 40, 1.0);
}